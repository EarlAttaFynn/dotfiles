# Zsh aliases for my headless systems
#
# currently used for "zephyr" pi server

export EDITOR=vim

alias docker='sudo docker'
alias update='sudo apt update && sudo apt upgrade -y'

alias ts='sudo tailscale up --advertise-exit-node --accept-dns=false'
alias rm='trash-put'
